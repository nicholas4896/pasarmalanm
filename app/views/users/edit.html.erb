<div class="users">
  <div class="users">
  <%= form_for(@user, url: user_path(@user), html: { id: "user-update-form" }) do |f| %>
    <h1 class="edit text-center">
      Update User
    </h1>


    <div class="form-group">
      <h4> <%= f.label :text_field, "Update Username" %> </h4>
       <%= f.text_field :username, required: true, id: "username_field", class: 'form-control' %>
     </h4>
      <h4>
        <%= f.label :title_field, "Update Email." %>
      </h4>
        <%= f.text_field :email, required: true, id: "user_email_field", class: 'form-control' %>
      <h4>
        <%= f.label :password_field, "Update password." %>
      </h4>
        <%= f.password_field :password, required: true, id: "user_password_field", class: 'form-control' %>
    </div>

    <div class="form-group">
        <h4 class="text-center"> User Image </h4>
        <div id="preview-image">
          <% if @user.image? %>
            <%= image_tag @user.image, class: "img-settings img-responsive center-block" %>
          <% end %>
        </div>
      </div>

    <div class="form-group">
      <%= f.file_field :image, id: "user_image_field", class: 'form-control image-uploader' %>
    </div>

    <div class="form-group text-center">
      <%= f.submit "Update Credentials", class: "btn btn-success btn-lg" %>
    </div>
  <% end %>
  </div>
</div>
